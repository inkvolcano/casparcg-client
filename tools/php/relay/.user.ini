; Read by PHP-FPM and PHP-CGI before relay.php runs, which matters because some
; warnings are emitted at request startup and no code inside the file can catch
; them. One of those in front of a JSON answer breaks every client, for a reason
; nobody would guess from the symptom.
;
; Errors still reach the host's log.
display_errors = 0
log_errors = 1
